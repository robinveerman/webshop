{% extends 'base2.html.twig' %}

{% block title %}Regel{% endblock %}

{% block body %}
    <h1>Regel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ regel.id }}</td>
            </tr>
            <tr>
                <th>Aantal</th>
                <td>{{ regel.aantal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('regel_index') }}" class="btn btn-primary">Terug</a>
<br>
    <a class="btn btn-warning" href="{{ path('regel_edit', {'id': regel.id}) }}">Bewerken</a>
<br>
<br>
    {{ include('regel/_delete_form.html.twig') }}
{% endblock %}
