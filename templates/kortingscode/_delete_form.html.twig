<form method="post" action="{{ path('kortingscode_delete', {'id': kortingscode.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kortingscode.id) }}">
    <button class="btn btn-danger">Verwijderen</button>
</form>
